<?php

class AppController extends Controller {
	
	/* save_imagethumb
	 * @param $imagepath string
	 * @param $x int
	 * @param $y int
	 * 指定された画像のサムネイルを生成し、パスをファイル名を返却する
	 */
	public static function save_imagethumb($imagepath = '',$x = '',$y = ''){
		return TRUE;
	}
}